#include <stdio.h>  

/**                                              * main - Entry point as the main function       *                                               * Return: Always 0                              */
int main(void)
{
	char text[] = "\"with proper grammar, but the outcome is a piece of art";

	printf(text);
	return (0);
}
