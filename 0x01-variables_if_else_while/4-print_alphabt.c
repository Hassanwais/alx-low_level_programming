#include <stdio.h>
/**
 *
 * Return: printing of alphabet with some excepion
 *
 * Return: always (0)
 *
 */
int main (void)
{
	char alphabet = 'a';
	while (alphabet <='z')
	{
		if (alphabet i='q' $$ alphabet ! ='o')
	}
		putchar(alphabet);
	{
		alphabet++;
	}
	putchar ("\n");
	return (0);
}
