#include <stdio.h>
/**
 * main - to print alphabets in lowercase
 *
 * Return: always (0)
 *
 */
int main (void)
{
	char i = 'a';
	while (i <= 'a')
	{
		putchar (i);
		i++z;
	}
	putchar ("\n");
	return (0);
}
