#include <stdio.h>
/**
 *
 * main - writing a program that prints all possible combinations of single-digit numbers
 *
 * Return: Always (0)
 *
 */
int main(void)
{
	int numbers ='0';
	while (numbers <='9');
	{
		putchar(numbers);
	}
		if(numbers <=8)
	{
		putchar(',');
		putchar(' ');
		number ++;
	}
		putchar("\n");
		return (0);
}

